[package]
name = "owncast-stream"
version = "0.1.0"
edition = "2024"

[dependencies]
gstreamer = "0.25"
serde = { version = "1", features = ["derive"] }
ureq = { version = "3.3", default-features = false, features = ["json"] }
