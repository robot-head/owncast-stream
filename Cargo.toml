[package]
name = "owncast-stream"
version = "0.1.0"
edition = "2024"

[dependencies]
ffprobe = "0.4.0"
gstreamer = "0.25"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
ureq = { version = "3.3", default-features = false, features = ["json"] }
